benchmarks:
  "^GSPC": "S&P 500 Index (US)"

default_benchmark: "^GSPC"

risk_free_rate_annual: 0.04  # 4% per year

refresh_interval:
  market_open: 60      # seconds between intraday ticks when market is open
  market_closed: 900   # seconds between checks when market is closed
